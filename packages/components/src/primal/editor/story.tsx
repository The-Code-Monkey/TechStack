import { Meta } from '@storybook/react';

import Editor from './Editor';

export default {
  title: 'Components / Primal / Editor',
  component: Editor,
} satisfies Meta<typeof Editor>;

export const Default = {};
