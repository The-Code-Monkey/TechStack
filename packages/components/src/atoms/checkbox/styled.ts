import { BoxProps } from '../../primatives';

export const disabledStyles: BoxProps = {
  bgColor: 'highlights.0',
  borderColor: 'neutrals.19',
};

export const checkedStyles: BoxProps = {
  opacity: '3',
};
